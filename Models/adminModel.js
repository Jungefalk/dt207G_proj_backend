/**
 * Schema och Model för Admin
 */

const mongoose = require("mongoose");

//Schema
const adminSchema = new mongoose.Schema({
    username: {
        type: String,
        required: true,
        unique: true,
        trim: true
    },
    password: {
        type: String,
        required: true
    }
});